import blockContent from './blockContent'
import category from './category'
import post from './post'
import timeline from './timeline'

export const schemaTypes = [post, category, blockContent, timeline]
